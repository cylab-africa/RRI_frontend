apiVersion: v1
kind: Service
metadata:
  name: {{ include "rri_frontend.fullname" . }}
  labels:
    {{- include "rri_frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rri_frontend.selectorLabels" . | nindent 4 }}
